# UCM2 card-driver match for Universal Audio Apollo
# UCM2 lookup: conf.d/${CardDriver}/${CardDriver}.conf
# On deployment, create a symlink instead of copying this file:
#   ln -sf ../../ua_apollo/ua_apollo.conf /usr/share/alsa/ucm2/conf.d/ua_apollo/ua_apollo.conf

Include "../../ua_apollo/ua_apollo.conf"
