# UCM2 profile for Universal Audio Apollo interfaces
# Deploy to: /usr/share/alsa/ucm2/ua_apollo/ua_apollo.conf
# Symlink from: /usr/share/alsa/ucm2/conf.d/ua_apollo/{card_id}.conf

Syntax 6

SectionUseCase."HiFi" {
	File "HiFi.conf"
	Comment "Default audio (Monitor stereo out + Mic stereo in)"
}
